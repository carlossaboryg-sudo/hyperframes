---
description: Lint HyperFrames composition for structural issues
agent: hf-lint
subtask: true
---

Run `npx hyperframes lint` on the project directory $ARGUMENTS.
Report any errors and warnings found. Suggest fixes for each issue.
If $ARGUMENTS is empty, lint the current project directory.
